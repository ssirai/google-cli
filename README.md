trivial command line interface for google search made by click.

usage:

```bash
$ pip install git@github.com/ssirai/google-cli
$ google python
(opening your default browser)
$ google python\ click  # space delimited search needs escaping '\'
```

